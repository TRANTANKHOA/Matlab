% PRIVATE
%
% Files
%   cases    - 
%   cholorth - Computes permuted lower Cholesky factor L for Sig
%   cholperm - Computes permuted lower Cholesky factor L for Sig
%   gradpsi  - implements gradient of psi(x) to find optimal exponential twisting;
%   gradpsiT - implements gradient of psi(x) to find optimal exponential twisting;
%   lnNpr    - computes ln(P(a<Z<b))
%   lnPhi    - computes logarithm of  tail of Z~N(0,1) mitigating
%   mvnpr    - computes P(l<X<u), where X is normal with
%   mvnprqmc - computes P(l<X<u), where X is normal with
%   mvnrnd   - generates the proposals from the exponentially tilted 
%   mvtpr    - computes P(l<X<u), where X is student with
%   mvtprqmc - computes P(l<X<u), where X is student with
%   mvtrnd   - generates the proposals from the exponentially tilted 
%   newton   - 
%   normq    - computes with precision the quantile function
%   ntail    - samples a column vector of length=length(l)=length(u)
%   Phinv    - computes with precision the quantile function
%   psy      - implements psi(x,mu); assumes scaled 'L' without diagonal;
%   psyT     - implements psi(x,mu); assumes scaled 'L' without diagonal;
%   tn       - samples a column vector of length=length(l)=length(u)
%   trnd     - uses acceptance rejection to simulate from truncated normal
